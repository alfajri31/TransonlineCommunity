
@{
    ViewBag.Title = "Login Administrator";

}

@using (Html.BeginForm())
{
<section class="vh-100" style="background: linear-gradient(110deg, #fdcd3b 60%, #ffed4b 60%); ">
    <div class="container pt-sm-5">
        <div class="row d-flex justify-content-center align-items-center h-100">
            <div class="col col-xl-10">
                <div class="card" style="border-radius: 1rem;">
                    <div class="row g-0">
                        <div class="col-md-6 col-lg-5 d-none d-md-block">
                            <img src="https://mdbootstrap.com/img/Photos/new-templates/bootstrap-login-form/img1.jpg"
                                 alt="login form"
                                 class="img-fluid" style="border-radius: 1rem 0 0 1rem;" />
                        </div>
                        <div class="col-md-6 col-lg-7 d-flex align-items-center">
                            <div class="card-body p-4 p-lg-5 text-black">

                                <form method="post">
                                    @Html.AntiForgeryToken()
                                    @Html.ValidationSummary()
                                    <div class="d-flex align-items-center mb-3 pb-1">
                                        <i class="fas fa-cubes fa-2x me-auto" style="color: #ff6219;"></i>
                                        <span class="h1 fw-bold mb-0">Admin Portal</span>
                                    </div>

                                    <h5 class="fw-normal mb-3 pb-3" style="letter-spacing: 1px;">Sign into your account</h5>

                                    <div class="form-outline mb-4">
                                        <input type="text" id="username" name="username" class="form-control form-control-lg" value="@Request.Form["username"]" />
                                        @Html.ValidationMessage("username")
                                        <label class="form-label" for="Username">Username</label>
                                    </div>

                                    @*<div class="form-outline mb-4">
                                            <input type="email" id="email" name="email" class="form-control form-control-lg" value="@Request.Form["email"]" />
                                            @Html.ValidationMessage("email")
                                            <label class="form-label" for="email">Email address</label>
                                        </div>*@

                                    <div class="form-outline mb-4">
                                        <input type="password" id="password" name="password" class="form-control form-control-lg" value="@Request.Form["password"]" />
                                        @Html.ValidationMessage("password")
                                        <label class="form-label" for="Password">Password</label>
                                    </div>

                                    <div class="pt-1 mb-4" id="login-content">
                                        <button id="button-login" class="btn btn-dark btn-lg btn-block" type="submit">Login</button>
                                    </div>

                                    @*<div class="custom-loading-login-normal"></div>*@

                                    @*<a class="small text-muted" href="#!">Forgot password?</a>
                                        <p class="mb-5 pb-lg-2" style="color: #393f81;">Don't have an account? <a href="#!" style="color: #393f81;">Register here</a></p>*@
                                    <a href="#!" class="small text-muted">Terms of use.</a>
                                    <a href="#!" class="small text-muted">Privacy policy</a>
                                </form>

                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
}




@*<script>
        var button = document.querySelector('button');
        button.onclick = function () {
            $("#button-login").remove();
            $("#login-content").addClass("custom-loading-login-normal");
            //do stuff
            $.ajax({
                type: "POST", //HTTP POST Method
                url: "/LoginAdministrator/CheckIn", // Controller/View
                data: { //Passing data
                    Username: $("#username").val(), //Reading text box values using Jquery
                    Email: $("#email").val(),
                    Password: $("#password").val()
                },
                success: function () {

                    window.location = "/Home";
                }
            })
        }
    </script>*@

<script>
        var button = document.querySelector('#button-login');
    button.onclick = function () {
        $("#login-content").append('<div class="custom-loading-login-normal"></div>')
        }
    </script>